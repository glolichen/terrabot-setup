Use `sudo visudo` to add `Defaults    timestamp_timeout=30` to sudoers file
Then `wget https://raw.githubusercontent.com/glolichen/terrabot-setup/main/setup.sh` to `/home/robotanist`
`chmod +x setup.sh`
`./setup.sh`
